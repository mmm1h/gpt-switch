{
  "name": "gpt-account-switcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "dev": "vite build --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^25.6.2",
    "typescript": "^5.7.2",
    "vite": "^8.0.11",
    "vitest": "^4.1.5"
  }
}
