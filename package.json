{
  "name": "gpt-account-switcher",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "package:zip": "node scripts/create-extension-zip.mjs",
    "test": "vitest run",
    "e2e": "npm run build && playwright test -c playwright.config.ts",
    "e2e:chrome": "npm run build && playwright test -c playwright.chrome.config.ts",
    "chrome:debug": "powershell -ExecutionPolicy Bypass -File scripts/start-debug-chrome.ps1",
    "dev": "vite build --watch"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/chrome": "^0.0.287",
    "@types/node": "^25.6.2",
    "typescript": "^5.7.2",
    "vite": "^8.0.11",
    "vitest": "^4.1.5"
  }
}
